# Phoenix Configuration
# Copy this file to .env and fill in your values
# Never commit .env to version control

# Phoenix server configuration
PHX_HOST=localhost
PORT=4000

# Secret key base (generate with: mix phx.gen.secret)
# SECRET_KEY_BASE=your_secret_key_here

# Database configuration
# For local development (Docker):
DATABASE_URL=postgres://postgres:postgres@localhost/jarga_dev

# For Neon DB (production/staging), use this format instead:
# DATABASE_URL=postgres://username:password@host:port/database?sslmode=require

# Performance tuning
# PAGE_SAVE_DEBOUNCE_MS=2000  # Debounce time for page auto-save (default: 2000ms)
# Note: .env.test sets this to 1ms for faster test runs

# External API keys
# Add your API keys and secrets here
