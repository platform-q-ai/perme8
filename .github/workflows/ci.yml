name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  MIX_ENV: test
  SKIP_CREDO_CHECK: 1
  PAGE_SAVE_DEBOUNCE_MS: 1

jobs:
  changes:
    name: Detect changes
    runs-on: ubuntu-latest
    outputs:
      exo_bdd: ${{ steps.filter.outputs.exo_bdd }}
      umbrella: ${{ steps.filter.outputs.umbrella }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            exo_bdd:
              - 'tools/exo-bdd/**'
              - '!tools/exo-bdd/**/*.md'
            umbrella:
              - '**'
              - '!tools/exo-bdd/**'
              - '!**/*.md'
              - '!docs/**'
              - '!LICENSE*'
              - '!.Claude/**'

  exo-bdd:
    name: exo-bdd tests
    needs: changes
    if: needs.changes.outputs.exo_bdd == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install
        working-directory: tools/exo-bdd

      - name: Run unit tests
        run: bun test tests/
        working-directory: tools/exo-bdd

      - name: Run integration tests
        run: bun test integration/
        working-directory: tools/exo-bdd

  test:
    name: Build and test
    needs: changes
    if: needs.changes.outputs.umbrella == 'true'
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: jarga_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          version-file: .tool-versions
          version-type: strict

      - name: Restore deps cache
        uses: actions/cache@v4
        with:
          path: deps
          key: ${{ runner.os }}-deps-${{ hashFiles('.tool-versions', '**/mix.lock') }}
          restore-keys: ${{ runner.os }}-deps-

      - name: Restore build cache
        uses: actions/cache@v4
        with:
          path: _build
          key: ${{ runner.os }}-build-${{ hashFiles('.tool-versions', '**/mix.lock') }}-${{ hashFiles('apps/**/lib/**/*.ex', 'apps/**/mix.exs') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ hashFiles('.tool-versions', '**/mix.lock') }}-
            ${{ runner.os }}-build-

      - name: Install dependencies
        run: mix deps.get

      - name: Compile dependencies
        run: mix deps.compile

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .tool-versions

      - name: Install JavaScript dependencies
        run: |
          cd apps/jarga_web/assets
          npm ci
          cd ../../identity/assets
          npm ci

      - name: Create test database
        run: MIX_ENV=test mix ecto.create
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/jarga_test

      - name: Run database migrations
        run: MIX_ENV=test mix ecto.migrate
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/jarga_test

      - name: Run precommit checks
        run: mix precommit
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/jarga_test

  exo-bdd-360:
    name: "360: ${{ matrix.domain }} / ${{ matrix.app }}"
    needs: [changes]
    if: needs.changes.outputs.umbrella == 'true' || needs.changes.outputs.exo_bdd == 'true'
    timeout-minutes: ${{ matrix.timeout }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        app: [alkali, erm, identity, jarga-api, jarga-web]
        domain: [browser, cli, http, security]
        exclude:
          # alkali only has CLI features
          - app: alkali
            domain: browser
          - app: alkali
            domain: http
          - app: alkali
            domain: security
          # ERM has HTTP and security features
          - app: erm
            domain: browser
          - app: erm
            domain: cli
          # identity has browser and security features
          - app: identity
            domain: cli
          - app: identity
            domain: http
          # jarga-api has HTTP and security features
          - app: jarga-api
            domain: browser
          - app: jarga-api
            domain: cli
          # jarga-web only has browser features
          - app: jarga-web
            domain: cli
          - app: jarga-web
            domain: http
          - app: jarga-web
            domain: security
        include:
          # Map app names to exo-bdd config stems where they differ
          - app: erm
            config_name: entity-relationship-manager
          # Timeouts per domain
          - domain: browser
            timeout: 15
          - domain: cli
            timeout: 5
          - domain: http
            timeout: 10
          - domain: security
            timeout: 15

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: jarga_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          version-file: .tool-versions
          version-type: strict

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2

      - name: Restore deps cache
        uses: actions/cache@v4
        with:
          path: deps
          key: ${{ runner.os }}-deps-${{ hashFiles('.tool-versions', '**/mix.lock') }}
          restore-keys: ${{ runner.os }}-deps-

      - name: Restore build cache
        uses: actions/cache@v4
        with:
          path: _build
          key: ${{ runner.os }}-build-${{ hashFiles('.tool-versions', '**/mix.lock') }}-${{ hashFiles('apps/**/lib/**/*.ex', 'apps/**/mix.exs') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ hashFiles('.tool-versions', '**/mix.lock') }}-
            ${{ runner.os }}-build-

      - name: Install Elixir dependencies
        run: mix deps.get

      - name: Compile dependencies
        run: mix deps.compile

      - name: Set up Node.js
        if: matrix.domain == 'browser'
        uses: actions/setup-node@v4
        with:
          node-version-file: .tool-versions

      - name: Install exo-bdd dependencies
        run: bun install
        working-directory: tools/exo-bdd

      - name: Install JavaScript dependencies
        if: matrix.domain == 'browser'
        run: |
          cd apps/jarga_web/assets && npm ci
          cd ../../identity/assets && npm ci

      - name: Install Playwright browsers
        if: matrix.domain == 'browser'
        run: bunx playwright install --with-deps chromium
        working-directory: tools/exo-bdd

      - name: Build assets
        if: matrix.domain == 'browser'
        run: mix assets.build

      - name: Create test database
        if: matrix.app != 'alkali'
        run: MIX_ENV=test mix ecto.create
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/jarga_test

      - name: Run database migrations
        if: matrix.app != 'alkali'
        run: MIX_ENV=test mix ecto.migrate
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/jarga_test

      - name: Run exo-bdd tests
        run: mix exo_test --name ${{ matrix.config_name || matrix.app }} --adapter ${{ matrix.domain }}
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/jarga_test

  deploy:
    name: Deploy to Render
    runs-on: ubuntu-latest
    needs: [changes, test, exo-bdd, exo-bdd-360]
    if: |
      always() &&
      github.event_name == 'push' && github.ref == 'refs/heads/main' &&
      !contains(needs.*.result, 'failure') &&
      !contains(needs.*.result, 'cancelled')

    steps:
      - name: Deploy
        env:
          deploy_url: ${{ secrets.RENDER_DEPLOY_HOOK_URL }}
        run: |
          curl "$deploy_url"
